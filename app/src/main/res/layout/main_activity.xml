<?xml version="1.0" encoding="utf-8"?>
<ScrollView
	xmlns:android="http://schemas.android.com/apk/res/android"
	style="@style/full"
	android:fillViewport="true">

	<LinearLayout
		style="@style/root">

		<Button
			style="@style/fill"
			android:text="@string/openRom"
			android:id="@+id/menu_openRom"
			android:layout_height="100dp"/>

		<Spinner
			style="@style/fill"
			android:id="@+id/functionView"/>

		<LinearLayout
			style="@style/root"
			android:id="@+id/layout">

		</LinearLayout>

	</LinearLayout>

</ScrollView>

